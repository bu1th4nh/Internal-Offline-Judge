//Libraries and namespaces
//#include <bits/stdc++.h>
#include <algorithm>
#include <bitset>
#include <cmath>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <fstream>
#include <functional>
#include <iostream>
#include <iomanip>
#include <map>
#include <queue>
#include <set>
#include <sstream>
#include <stack>
#include <string>
#include <vector>
#include <utility>
using namespace std;


//=====================================
//Macros
#define task "1"
#define fi first
#define se second
#define maxinp (int)()
#define MODUL (int)(1e9+57)
#define siz(x) (int)(x.size())
#define len(x) (int)(x.length())
#define whole(x) x.begin(), x.end()
#define FOR(i, x, y) for(int i=x; i<=y; ++i)
#define FORl(i, x, y) for(int i=x; i<y; ++i)
#define FORb(i, x, y) for(int i=x; i>=y; --i)
#define FORlb(i, x, y) for(int i=x; i>y; --i)
#define MEMS(x, val) memset(x, val, sizeof(x))
#define FILEOP() freopen(task".in", "w", stdout); freopen(task".out", "w", stdout);

//=====================================
//Typedef
typedef unsigned long long ull;
typedef pair<int, int> ii;
typedef vector<int> vi;
typedef long long ll;

//=====================================
//Functions and procedures
//Main Procedure
int main()
{
    freopen("1.in", "w", stdout); freopen("1.out", "w", stdout);
    freopen("2.in", "w", stdout); freopen("2.out", "w", stdout);
    freopen("3.in", "w", stdout); freopen("3.out", "w", stdout);
    freopen("4.in", "w", stdout); freopen("4.out", "w", stdout);
    freopen("5.in", "w", stdout); freopen("5.out", "w", stdout);
    freopen("6.in", "w", stdout); freopen("6.out", "w", stdout);
    freopen("7.in", "w", stdout); freopen("7.out", "w", stdout);
    freopen("8.in", "w", stdout); freopen("8.out", "w", stdout);
    freopen("9.in", "w", stdout); freopen("9.out", "w", stdout);
    return 0;
}
